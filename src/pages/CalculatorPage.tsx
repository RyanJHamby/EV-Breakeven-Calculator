import { MileageBreakevenCalculator } from '../components/calculator/MileageBreakevenCalculator';

export default function CalculatorPage() {
    return (
        <div>
            <MileageBreakevenCalculator />
        </div>
    );
}